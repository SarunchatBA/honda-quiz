@model IEnumerable<ApproveProject.Repository.HistoryTransaction>

@{
    ViewBag.Title = "Log";
}

<h2>Log</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoneyApprove)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Log)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoneyApprove)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Log)
        </td>
    </tr>
}

</table>
@Html.ActionLink("Back to List", "Index")
